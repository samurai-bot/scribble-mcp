[project]
name = "scribble-mcp"
version = "1.0.0"
description = "Direct HTTP API for the Ubuntu-mounted iCloud Obsidian vault"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
vault-api = "vault_api:main"
